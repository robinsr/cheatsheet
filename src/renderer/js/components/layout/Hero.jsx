import React from 'react';

const Hero = (props) => {
    return (
        <div className="hero-body text-center">
            <h1>Keymap Maker</h1>
            <p>Generates keyboard shortcut tables in HTML</p>
        </div>
    );
}

export default Hero;
